<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.Tfs_spring_2024_reshetnikov" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Tfs_spring_2024_reshetnikov" parent="Base.Theme.Tfs_spring_2024_reshetnikov" />

    <style name="ToolbarTheme" parent="@style/ThemeOverlay.AppCompat.ActionBar">
        <!-- Customize color of navigation drawer icon and back arrow -->
        <item name="colorControlNormal">@color/onContainer</item>
        <item name="titleTextStyle">@style/TextAppearance.Widget.ActionBar.Title</item>
    </style>

    <style name="TextAppearance.Widget.ActionBar.Title" parent="@style/TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">@color/white</item>
    </style>
</resources>
